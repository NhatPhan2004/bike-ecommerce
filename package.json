{
  "name": "bike-ecommerce-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install",
    "client": "npm start --workspace=client",
    "server": "npm start --workspace=server"
  },
  "dependencies": {
    "@craco/craco": "^7.1.0"
  }
}
